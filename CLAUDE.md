# Leo记账 — 产品与技术设计文档

> 本文件是项目的"总纲"。后续所有开发都以此为准。
> 维护者原则:**用户(Leo)是编程小白**,所有技术决策必须由我(Claude)列出 2-4 个方案,逐条用大白话解释优劣,由用户拍板。**绝不擅自替用户做技术决定**。

---

## 一、产品概述

- **产品名称**:Leo记账(中文名),英文名 `leo-ledger` 或 `leo`。
- **一句话定位**:一个简单、好用、本地优先的个人记账 App,记录每一笔花销并按分类统计。
- **目标用户**:Leo 本人(初期),及其他想要一个轻量记账工具的个人用户。
- **核心价值**:操作够快、分类清晰、能看到自己钱花哪了,数据存在自己电脑上(隐私第一)。

---

## 二、功能需求

### 核心功能(第一期必须做)

1. **记一笔支出**
   - 输入:金额(人民币 ¥)、分类(二级)、可选备注、日期(默认今天)。
   - 一笔记录 = { 金额, 一级分类, 二级分类, 日期, 备注, 创建时间 }。

2. **分类体系(二级)**——由 Claude 设计如下(可在开发前微调):

   | 一级大类 | 二级小类 |
   |---|---|
   | 餐饮美食 | 早餐、午餐、晚餐、夜宵、零食饮料、外卖、聚餐 |
   | 交通出行 | 地铁公交、打车网约车、共享单车、火车机票、加油停车、过路费 |
   | 购物消费 | 日用百货、服装鞋帽、数码电器、美妆护肤、书本文具、母婴用品 |
   | 居家生活 | 房租房贷、水电燃气、物业宽带、家具家居、清洁保洁、绿植宠物 |
   | 娱乐休闲 | 电影演出、游戏充值、旅行度假、运动健身、爱好收藏、会员订阅 |
   | 医疗健康 | 门诊看病、药店买药、体检保健、牙科眼科、心理咨询 |
   | 人情社交 | 红白喜事、礼物红包、请客吃饭、聚会活动、捐赠公益 |
   | 学习成长 | 培训课程、书本资料、考试报名、知识付费、技能工具 |
   | 金融保险 | 保险保费、理财投资、信用卡年费、贷款还款、税费 |
   | 其他支出 | 杂项、待调整 |

   > 说明:二级分类用于更精细统计(共 53 个二级分类);一笔账必须选到二级,一级由二级自动归属。
   > 收入暂不记录(本期聚焦"花销"),如后续需要再加。

3. **查看记录**
   - 列表视图:按日期倒序展示所有支出,可按月份/一级分类/二级分类筛选。
   - 每条记录可编辑、删除。

4. **统计**
   - 本月总支出。
   - 按一级分类的支出占比(饼图)。
   - 按二级分类的支出明细(横向条形图)。
   - 日/周/月趋势(柱状图,可切换三种维度)。

### 第二期可做(先不做,仅规划)

- 多账本/多月度预算。
- 导出 Excel / CSV。
- 数据云同步。
- 收入记录与收支总览。
- 深色模式、移动端适配。

---

## 三、非功能要求

- **跨平台**:必须在 Windows 和 Mac 上都能运行(用户明确要求)。
- **本地优先**:数据默认存在用户自己电脑,不上传第三方服务器(隐私)。
- **离线可用**:不依赖网络即可记账。
- **简单**:Leo 是小白,UI 要清楚,操作步数少。
- **可长期维护**:技术栈要主流、社区大、资料多,出问题我能查得到/好求助。

---

## 四、技术栈选型(待用户决定)

跨平台桌面 App 有三条主流路线,我列出优劣势,请阅读后选择**一个**。

> 名词解释(先看这个再读下面表格):
> - **前端**:用户看到、点击的那一层界面(按钮、列表、图表)。
> - **后端**:处理数据、读写存档的逻辑。桌面 App 里后端就是"存数据的那部分代码"。
> - **框架**:一套帮你把前后端拼成一个完整 App 的工具包,省得你从零写。
> - **打包**:把代码变成别人电脑上能双击运行的 `.exe`(Windows)或 `.dmg`(Mac)文件。
> - **Web 技术(HTML/CSS/JavaScript)**:做网页用的那套语言,也能用来做桌面 App 的界面。
> - **原生**:专门为某个系统(Mac 或 Windows)量身写的程序,体验最好但要写两套。

### 方案 A:Electron + React + SQLite ⭐推荐给小白

| 维度 | 说明 |
|---|---|
| 怎么做 | 用网页技术(React)写界面,套一个 Electron 外壳变成桌面程序;数据存在本地 SQLite 数据库文件里。 |
| 优势 | ① 跨平台一套代码即跑 Windows+Mac;② 生态最成熟,网上教程/问答最多,出问题最好查;③ 界面好看、图表多;④ 你电脑已装好 Node/pnpm,能直接开工。 |
| 劣势 | ① App 体积偏大(约 80-150MB,因为它内嵌了一个浏览器);② 启动比原生略慢(1-2 秒);③ 内存占用偏高。 |
| 适合谁 | 想要好看界面 + 想长期扩展功能 + 不介意体积的人。**最适合 Leo 当前情况。** |

### 方案 B:Tauri + React + SQLite

| 维度 | 说明 |
|---|---|
| 怎么做 | 同样用网页技术写界面,但外壳用 Rust 语言写的 Tauri,直接调用系统自带浏览器,不内嵌。 |
| 优势 | ① 体积超小(5-15MB,Electron 的十分之一);② 内存省、启动快;③ 安全性更好。 |
| 优势的代价 | ① 必须装 Rust 工具链(你电脑现在没装,要再装一门新语言环境);② 生态比 Electron 新,中文教程相对少,踩坑时求助更难;③ 打包配置更复杂,小白上手门槛偏高。 |
| 适合谁 | 追求轻量、对体积敏感、且能接受多装一门 Rust 的人。 |

### 方案 C:Flutter + SQLite

| 维度 | 说明 |
|---|---|
| 怎么做 | 用 Google 的 Flutter 框架(Dart 语言)写一套代码,编译成 Windows 和 Mac 原生界面。 |
| 优势 | ① 一套代码同时覆盖桌面+手机(以后想出手机版很顺);② 性能好、界面流畅;③ Google 主推,长期活跃。 |
| 劣势 | ① 要学一门新语言 Dart + 新框架,小白学习曲线陡;② 桌面端资料比移动端少,中文桌面教程不多;③ 图表库要单独配,不如前两个方案现成。 |
| 适合谁 | 未来确定要做手机版、并且愿意学新语言的人。 |

### 一句话对比

- 想省心、资料多、能直接开干 → **方案 A (Electron)**
- 想要轻巧小巧、不怕多装个 Rust → 方案 B (Tauri)
- 以后要做手机版、肯学新东西 → 方案 C (Flutter)

> 我的建议(仅供你参考,以你的决定为准):**选方案 A**。因为你是初学者,Electron 资料最丰富、不需要装新语言、你电脑已经具备开工条件,能把"先跑起来看到东西"这件事做到最快,这对建立信心最重要。

---

## 五、数据存储

- **数据库**:SQLite(一个本地文件,如 `leo.db`)。
  - 为什么:轻量、无需安装服务、单文件易备份、跨平台一致。
- **备份**:数据库文件可直接复制带走,后续可加"一键导出"。

---

### ✅ 已选定:方案 A — Electron + React + SQLite

**用户决定(2026-08-03)**:采用方案 A。理由:资料最丰富、无需安装新语言、本机已具备开工条件,能最快"先跑起来看到东西",对初学者最友好。

**技术栈清单**:
- 界面:React 18 + TypeScript(类型安全的 JavaScript)
- 构建:electron-vite + Vite 7(打包工具,比传统脚手架快)
- 桌面外壳:Electron 33
- 数据库:SQLite,通过 sql.js(纯 WebAssembly)驱动
  - 踩坑记录:最初选 better-sqlite3,需本机 C++ 工具链(Visual Studio Build Tools),用户机器未装、编译失败。改用 sql.js,零编译即可运行;数据访问层抽在 `electron/db.ts`,将来装好工具链后只改该文件即可换回 better-sqlite3。
- 图表:Recharts 3(基于 React,上手简单)
- UI 组件:基础 CSS + 少量样式,不引入重型组件库

**已知取舍**:
- App 体积约 80-150MB,启动约 1-2 秒 —— 用户已知悉并接受。

**项目结构**(代码在 `leo-app/` 子目录):
```
leo-app/
├─ electron/            主进程(Node 环境,管数据 & 创建窗口)
│  ├─ main.ts          Electron 入口,创建窗口 + 注册 IPC
│  ├─ preload.ts       桥接脚本,向界面暴露 window.leo.*
│  └─ db.ts            数据访问层(唯一耦合 SQLite 驱动的地方)
├─ src/                 渲染进程(界面,React)
│  ├─ App.tsx          主界面:记一笔 / 月份切换 / 列表 / 图表
│  ├─ App.css / index.css  样式
│  ├─ main.tsx         React 挂载入口
│  └─ leo.d.ts         window.leo 的 TypeScript 类型声明
├─ index.html          网页壳
├─ electron.vite.config.ts  electron-vite 配置(main/preload/renderer)
├─ package.json        依赖清单 + 启动脚本
└─ out/                构建产物(git 忽略)
```

**启动方式**:`cd leo-app && npm run dev`(开发,热更新窗口) / `npm run build`(生产打包前的本体构建)。
**数据库位置**:sql.js 把数据写入用户数据目录下的 `leo.db`(%AppData%/leo-app/leo.db)。

---

## 七、开发流程约定(给小白用户)

1. **决策方式**:凡涉及选型、目录结构、库的选择、是否引入新功能等"技术决定",我(Claude)一律先列 2-4 个方案 + 优劣解释,等用户选定后才动手,绝不擅自决定。
2. **解释方式**:技术名词首次出现时,用大白话在括号里解释一次。
3. **进度方式**:每完成一步,我用一句话告诉你做了什么、下一步是什么。
4. **不擅自重构**:遇到能顺手改但不影响功能的地方,先问,不自行扩大改动。
5. **代码注释**:只在"这里为什么这么写"不显然时才写,不写流水账注释。

---

## 八、待用户确认事项(开发开始前)

- [x] **技术栈方案**:A — Electron + React + SQLite ✅(2026-08-03 决定)
- [x] SQLite 驱动:sql.js ✅(2026-08-03,better-sqlite3 编译失败后改用)
- [x] 分类体系:照用已设计的 10 大类 52 小类 ✅(2026-08-03 确认)
- [ ] 是否需要"收入"功能(本期默认不做)。
- [x] 是否需要深色模式(已支持深色/浅色一键切换 ✅)。
- [x] App 显示名:leo记账 ✅(2026-08-04 打包配置 productName=leo记账;英文名 leo-ledger 暂未在包名使用)
- [x] 打包成 .exe 用于分发 ✅(2026-08-04,electron-builder + nsis,产出 release/leo-0.0.0-setup.exe;Mac .dmg 待在 Mac 机器上执行 `electron-builder --mac`)

---

## 九、变更记录

- 2026-08-03:用户选定技术栈为方案 A (Electron),更新第六节与待确认项。
- 2026-08-03:better-sqlite3 因缺 C++ 工具链编译失败,改用 sql.js;数据访问层隔离在 db.ts。
- 2026-08-03:完成项目骨架(electron-vite)与首期核心功能(记一笔/月份切换/列表改删/饼图/日趋势),dev 与 build 均验证通过。
- 2026-08-04:统计补全——二级分类明细(summaryByL2)接到界面、新增按周趋势(summaryByWeek),趋势图支持日/周切换;CLAUDE.md 二.4"日/周/月趋势"至此覆盖日、周两种。
- 2026-08-04:接入 electron-builder 打包,$ `npm run dist` 一键产出 release/leo-0.0.0-setup.exe(Windows NSIS 安装包,约 83MB)与免安装版 release/win-unpacked/leo记账.exe;sql-wasm.wasm 通过 asarUnpack 解出 asar 外确保运行时可读。未做代码签名(无证书),首次运行 Windows 会提示"未知发行商"。
- 2026-08-05:修复关键 bug——db.ts 中 5 处 `stmt.bind(值, 1)` 改为 `stmt.bind([值])`,sql.js 只认数组/对象参数,原写法导致查询条件恒为 NULL、列表与图表始终为空。补齐核心功能:列表支持一级/二级分类筛选(二.3);新增 `summaryByMonth` 与月趋势图(二.4 完整覆盖日/周/月三档);修正 `summaryByWeek` 时区偏移(改用本地 ymd 函数)。数据层通过 9 组功能测试,electron-vite build 通过。核心功能(第一期)至此全部完成。
- 2026-08-05:全面体验与功能升级：数据层新增 `settings` 表与 CSV 导出/数据导入功能；新增月度预算设定与消费进度条；界面新增 KPI 统计看板（本月总支出、日均开销、最高单笔、总笔数）、快捷记账按钮、搜索框与多维度排序；完成全套 UI 美化设计系统，支持深色/浅色模式一键切换。构建与打包打包全面验证通过。

